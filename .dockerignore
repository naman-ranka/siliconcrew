.venv/
venv/
__pycache__/
*.pyc
.env
.env.*
!.env.example
gcp-key.json
deploy/terraform/.terraform/
deploy/terraform/terraform.tfstate*
deploy/terraform/terraform.tfvars
.git/
.github/
.claude/
.codex/
.codex_local/
.agents/
.antigravitycli/
.cache/
.pytest_cache/
.vscode/
workspace/
workspace*/
# Locally bootstrapped PDK models. They moved out of workspace/ (which this
# file already excluded) to the install root in issue #59, so without this a
# developer's local cache — ~630 files — rides into every build context, and a
# lean BAKE_STDCELLS=0 image would ship it instead of the pinned download.
/_stdcells/
C/
data/
node_modules/
frontend/node_modules/
frontend/.next/
codex-automate/
cvdp-automate/
cvdp_benchmark/
ASU-Spec2Tapeout-ICLAD25-Hackathon/
sim_build/
paper/
docs/
*.zip
*.vcd
*.vvp
*.out
state.db
# Runtime data (bind-mounted, gitignored) — never part of the image. The Codex
# SDK writes special-mode temp files under here that also break the build tar.
.siliconcrew_data/
codex-state/
codex-sqlite/
state.db-wal
state.db-shm
p*.json
test_sby_output.txt
