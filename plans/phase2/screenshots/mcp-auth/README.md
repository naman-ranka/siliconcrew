# MCP remote-auth — live verification screenshots

Capture here when verifying the deployed WorkOS flow (no WorkOS creds in CI, so
this is a deploy-time step — see `deploy/MCP_REMOTE_AUTH.md`):

1. Claude app → add the deployed MCP URL → "Sign in with Google" via WorkOS.
2. Inside Claude: list / create / continue a session, run a tool.
3. The same session showing on the SiliconCrew website (one account, both places).
