# Generated by cvdp-pipeline/generate_cvdp_config.py — do not hand-edit problem ids.
# CVDP is RTL-simulation-only: the verilog flow + cvdp_agentic_jsonl kind instruct the
# agent to skip synthesis/PD. Validate with cvdp-pipeline/replay_cvdp_harness.py --run-dir.
# NOTE: for real (non-fake) agents, export RTL_WORKSPACE so the orchestrator's workspace
#       discovery matches where the rtl-codex MCP server writes sessions (its .env root).
name: cvdp_des_smoke
project:
  enabled: false
  name: bench_cvdp_des_smoke
defaults:
  agent: codex
  model: gpt-5.5
  flow: verilog
  mcp_server: rtl-codex
  timeout_sec: 2400
  evaluation: cvdp_replay_harness
problems:
- id: des_0001
  suite: cvdp
  kind: cvdp_agentic_jsonl
  dataset: C:/Users/naman/Desktop/Projects/RTL_AGENT/cvdp_benchmark/data/cvdp_v1.0.2_agentic_code_generation_no_commercial.jsonl
  datapoint_id: cvdp_agentic_DES_0001
